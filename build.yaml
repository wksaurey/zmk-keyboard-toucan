# This file generates the GitHub Actions matrix.
# For simple board + shield combinations, add them to the top level board and
# shield arrays, for more control, add individual board + shield combinations
# to the `include` property. You can also use the `cmake-args` property to
# pass flags to the build command, `snippet` to add a Zephyr snippet, and
# `artifact-name` to assign a name to distinguish build outputs from each other:
#
# board: [ "nice_nano_v2" ]
# shield: [ "corne_left", "corne_right" ]
# include:
#   - board: bdn9_rev2
#   - board: nice_nano_v2
#     shield: reviung41
#   - board: nice_nano_v2
#     shield: corne_left
#     snippet: studio-rpc-usb-uart
#     cmake-args: -DCONFIG_ZMK_STUDIO=y
#     artifact-name: corne_left_with_studio
#
---
include:
  # Trimmed to three images (Kolter, 2026-08-20): left, azoteq right,
  # settings_reset. The cirque right shield and the display style-1 left
  # variant are still BUILDABLE — re-add a matrix entry to get one:
  #   - board: seeeduino_xiao_ble
  #     shield: toucan_right rgbled_adapter
  #     snippet: zmk-usb-logging
  # (cirque revert normally uses the archived uf2s in firmware-archive/,
  # not a rebuild; display style 1 = add
  # -DCONFIG_TOUCAN_STATUS_SCREEN=1 + artifact-name to a left entry.)
  - board: seeeduino_xiao_ble
    shield: toucan_left rgbled_adapter nice_view_gem
    snippet: studio-rpc-usb-uart
    cmake-args: -DCONFIG_ZMK_STUDIO=y
  - board: seeeduino_xiao_ble
    shield: toucan_right_azoteq rgbled_adapter
    snippet: zmk-usb-logging
  - board: seeeduino_xiao_ble
    shield: settings_reset
